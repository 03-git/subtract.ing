lookdown.universal.tsv first-impression audit
==============================================
source: rousseau (sonnet 4.6), cold review 2026-04-27
status: unfixed

CRITICAL — command_not_found on fresh install
---------------------------------------------

1. kiwix-search (~150 entries)
   lines 361-435 (philosophical roots), 443-446, 1327-1505 (explain/read block)
   every kiwix-search call fails visibly without kiwix-tools installed.
   fix: fallback to echo with brief definition + "install kiwix-tools for offline search"

2. ~/subtract.ing/ hardcoded paths (12 entries)
   lines 324-326, 332-333, 337, 339-340, 342-343, 352-353
   reference dev repo clone, not ~/.subtract/ install path.
   fix: rewrite to use $SUBTRACT_DIR or ~/.subtract/fork/ paths

3. show*examples suggests "who is rousseau" (line 516)
   routes to kiwix-search which fails on fresh install.
   the first thing the file invites you to try is broken.
   fix: replace example with one that always works

4. minecraft-launcher apt claim (line 1675)
   "sudo apt install minecraft-launcher" — not in standard repos.
   fix: "download from minecraft.net"

SIGNIFICANT — silent failures
------------------------------

5. show*syslog (line 663)
   /var/log/syslog + log show both fail on modern journald debian.
   fix: add || journalctl -n 20

6. show*desktop / show*downloads (lines 34-35)
   ~/Desktop/ and ~/Downloads/ don't exist on headless/minimal.
   fix: add 2>/dev/null || echo "no Desktop folder"

7. battery_BAT0 hardcoded (line 494)
   specific UPower device name fails on most hardware.
   fix: upower -i $(upower -e | grep -i bat | head -1)

MINOR — functional but confusing
----------------------------------

8. WSL2 cmd.exe paths in search entries (lines 1664-1669)
   /mnt/c/Windows/System32/cmd.exe tried first, fails silently on non-WSL.
   functional via fallback but confusing to anyone reading the file.

9. SOUL.txt reference in what*is*a*system*prompt (line 447)
   formation-internal proper noun without context.

10. im*bored suggests kiwix-search (line 508)
    echo text recommends a command that may not exist.
